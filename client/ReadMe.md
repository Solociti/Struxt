## Bootstrap 5

Use the client/src/bootstrap directory to extend or modify any general bootstrap functionality.
It is assumed, that a general bootstrap import will include any style modifications from this directory.

When css is specific to a component, then either inline it, or setup a file adjacent to the component.
